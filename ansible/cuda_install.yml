---
- name: Install CUDA 9.2
  hosts: cybera
  become_method: sudo
  become_user: root

  tasks:
    - name: Check drive is mounted correctly
      stat:
        path: /mnt/data/cuda_9.2.88_396.26_linuxi
      register: stat_result
  
    - fail:
        msg: "Volume is not mounted with necessary files for CUDA"
      when: stat_result.stat.exists == False

    - name: Stop lighdm service
      become: yes
      service:
        name: lightdm
        state: stopped

    - name: Remove X1-lock file
      become: yes
      file:
        state: absent
        path: /tmp/.X1-lock
        
